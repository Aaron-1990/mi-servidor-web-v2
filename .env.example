# =============================================================================
# VSM PRODUCTION SYSTEM - ENVIRONMENT VARIABLES
# =============================================================================

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=vsm_production
DB_USER=vsm_admin
DB_PASSWORD=your_secure_password_here

# Server Configuration
PORT=3001
NODE_ENV=development

# Polling Intervals (milliseconds)
CSV_POLL_INTERVAL=30000
CALC_POLL_INTERVAL=60000

# CSV Fetch Configuration
CSV_TIMEOUT=10000
CSV_RETRY_ATTEMPTS=3

# Logging
LOG_LEVEL=info
LOG_FILE=./logs/app.log

# Data Retention (days)
RAW_SCANS_RETENTION_DAYS=30
